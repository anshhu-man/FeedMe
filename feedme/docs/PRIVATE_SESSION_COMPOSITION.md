# Private session composition — M1.05d.3d.3 / M1.05d.4

13 September 2026. `PrivateSessionRuntime` connects the existing private-data, credential, native-work and independent retirement components. It supplies an application-lifetime owner and durable exact-session binding. It is not an identity provider, bootstrap service, complete app UI, automatic guest merge or cloud synchronization. Original FeedMe scope remains 54 features and 98 screens.

Current continuation: [retained credential/data recovery owners](STARTUP_RECOVERY_OWNERS.md) extend the foundations behind [6c already-open confirmed abort](COMPOSITE_SETUP_ABORT.md). The runtime still borrows already-open resources; this new owner slice does not alter its publication/confirmation behavior. Task6d remains IN_PROGRESS for existing-only work recovery, startup composition and owned-close-before-Complete integration; task7 integrated interruption acceptance remains open. [Build status](BUILD_STATUS.md#current-verification) indexes current evidence; the original package counts below remain historical.

## Delivery packages

| Package | Status | Required proof |
| --- | --- | --- |
| M1.05d.3d.3a | DONE | Bounded shared component: 22 activation-codec and 20 runtime integration tests pass; exact binding, create/restore/cancel/retire/close, scoped access and offline-private gates, with an explicit test verifier |
| M1.05d.3d.3b | DONE | Bounded Android integration: four actual public-factory/OS tests pass, including runtime close/reopen/restore, pending retirement and newer-login protection; synthetic identity verification, not real provider proof |
| M1.05d.3d.3c | TODO | Actual approved provider and explicit FeedMe owner/bootstrap mapping, platform root/UI navigation, safe recovery UX and both-platform lifecycle evidence |

M1.05d.3d.3 remains in progress. Native domain receivers/workers, full guided cooking and every release gate remain separately tracked. A synthetic test verifier never enables the application's login controls.

## Why an exact binding is necessary

Independent credential, data and work stores can all contain the same account ID while belonging to different logins. Joining their scope strings after a crash could associate newer credentials with older cached meals or timers. The runtime therefore persists an exact binding inside the private owner database, at reserved internal key `session-activation/binding-v1`. New setup uses schema 2 with the exact setup operation UUID; legacy schema-1 records retain their original encoding and separately verified restore path. The key name is not the payload version.

It binds storage scope, credential incarnation UUID, work-origin UUID, the current authenticated 137-byte data-retirement target, an immutable trusted-configuration digest and, for schema 2, the setup operation matched against independent `Complete`. Credential refresh revision is deliberately excluded: a valid refresh of the same incarnation does not rebind existing commands. The record has a 4 KiB limit, strict exact keys/Unicode/UUIDs, a 64-character lowercase configuration digest and exact lowercase target encoding. It contains no password, access token, refresh token, guest proof or executable URL. Structural decoding alone never authenticates the target. Pre-publication replay preserves exact payload and increments its encrypted record revision; it never overwrites another binding.

The configuration digest must be generated by the trusted native configuration pipeline from the actual approved API environment, issuer, client/callback configuration and owner-mapping contract. It is not a user-selected environment label, permission to trust arbitrary endpoints or a substitute for provider verification. No production provider/configuration has been selected or deployed.

This active binding can live with private data because explicit logout first persists all exact cleanup targets in the independent retirement control ledger. Once that barrier exists, erasing the active binding cannot destroy cleanup authority. Interrupted initial setup without a complete binding is a blocked repair state; the runtime does not reconstruct the record by matching scopes, overwrite an existing guest, delete unknown keys, or invent an activation journal entry on restart.

## Application integration

The trusted platform root owns one serialized dispatcher, one `SessionBoundary`, the native credential store, independent control/work stores, private database, actual cancellation adapter, fresh UUID source and a reviewed domain execution policy. It opens one runtime and closes all native stores separately after closing it. No other component activates that boundary or writes the credential slot. UI receives presentation metadata; native repositories receive `PrivateSessionAccess`, never provider secrets.

`NativeSessionVerifier` is mandatory and has no default success implementation. Its `acquire` must finish actual provider verification, explicit FeedMe owner mapping and account bootstrap (or the genuine guest-session protocol) before returning credentials. Accounts without a registered device-session ID are rejected by this composition layer. F47's provider state/nonce/PKCE/issuer/audience checks, challenge routing and lost bootstrap response reconciliation are still the real adapter's responsibility. Bootstrap's current DTO does not provide an account ID: do not infer ownership from email or `Profile.id`; the owner-mapping contract still needs resolution before live use.

`restore` receives old secrets only after local binding/configuration/data/work checks pass. A configuration change must fail without handing old credentials to a verifier attached to a different provider or API. The verifier may return `OFFLINE_PRIVATE` only for explicitly permitted, previously verified local use. It cannot infer continuing server membership, recalls or account status while offline.

| Entry point | Actual behavior |
| --- | --- |
| `open` | Checks configuration digest, separate control/work objects, idle boundary and exclusive work-manager ownership; does not authenticate or activate |
| `recover` | Keeps outward access closed, finishes exact ordinary retirement and classifies signed-out/restore-required/recovery-required state; distinct pending CREATE and composite setup remain gated, never an automatic abort or lease restore |
| `create` | Requires signed-out and planned-credential capability, calls the real verifier, rejects occupied resources, acknowledges one exact credential/data/work journal before selection, then requires changed binding, work seal and final control acknowledgements before late access |
| `retryCreate` | Uses only the same retained verified live attempt and its original plans after a non-cancellation failure; no provider call, replacement identity or persisted-only reconstruction |
| `restore` | Requires restore-required; verifies exact binding/configuration/data/work and schema-2 control operation before old secrets reach the verifier; obtains a fresh changed control acknowledgement before bound data access and the first lease, then reconciles incomplete work and captures retirement |
| `cancelVerification` | Invalidates the current attempt and provisional lease immediately; late verifier results cannot write/activate; possibly committed initial writes become recovery, not guessed rollback |
| `retire` | Requires the exact published access and a fresh operation ID; closes admission, invokes the exact independent retirement barrier and retains pending cleanup on failure |
| `close` | Terminal in-memory closure and stale-handle fence, not implicit logout; closes its work registry while retaining durable state for a later verified restore |

The native stores are not one transaction. A failure after credential/data/work creation can leave evidence requiring user-visible repair. There is no automatic retry with fresh identity, silent guest conversion, compensating deletion of a current account, or proof of universal crash-safe initial setup. Same-process cancellation after a possibly committed write is conservatively fenced.

[Planned credential creation](PLANNED_CREDENTIAL_CREATE.md) supplies the mandatory `PlannedCredentialCreateStore`, with no legacy-create fallback. The current [composite publication protocol](SESSION_SETUP_PUBLICATION.md) prepares all three native plans, acknowledges one exact `PendingSetup` before material writes, and preserves it through ordered selection and changed binding/work-seal acknowledgements. Only changed final `Complete` plus exact current-resource checks allows late publication. Matching control readback never promotes a failed or unknown synchronization to success.

[Planned private-data activation](PLANNED_STATE_ACTIVATION.md) supplies the runtime's `planActivation` and `commitPlannedActivation` selection primitives. Their selection-only `Unit` is not a private-store handle, retirement target, lease or durable completion receipt; the later reserved binding write supplies a changed SQL COMMIT and exact readback. The separate [planned state recovery](PLANNED_STATE_RECOVERY.md) component is implemented as bounded b.2b.2 DONE. Every empty-only abort commits an actually changed V2 receipt and consumed generation before exact key deletion, including ABORTED retries; historical same/foreign-owner receipt references cannot be reused as candidate keys. Its restricted handle exposes only inspect/abort/close, not a new session capability, and cannot remove a written binding.

The API27+ existing-only recovery factory rejects V1, existing journals and unknown children and never creates files/locks/keys/schema or runs GC. Normal opening alone may atomically migrate exact V1 to V2; normal open/resume retain their cleanup behavior, whereas purpose-fixed binding inspection/publication avoids GC. Returned-handle close can retry, but failed construction plus failed cleanup close remains a process-restart gate. Composite tasks 1–5 now cover planning through binding/final lease publication; confirmed startup/abort and integrated interruption acceptance remain tasks 6 and 7, and no recovery UI is supplied. A no-op selected replay still adds no fresh sync, while the earlier post-successful-COMMIT exception represents a lost application receipt, not a proven original sync failure. Six historical separate-invocation tests pass with injected VFS sync errors in the actual bundled engine, not OS errno failures. Whole-session power-loss proof remains absent; no universal extra-fsync rule or descriptor redesign is inferred.

Startup must inspect independent control before trying to open an ordinary credential store with partial inventory. A distinct `PendingCreate` blocks restore, repository/transport access, work admission and ordinary retirement/discard recovery. Its separate existing-only `CredentialCreateCoordinator` requires explicit abort confirmation or an already-confirmed durable abort request. It cannot decrypt credentials, create a lease, repair arbitrary orphan inventory or substitute ordinary cleanup targets. Exact native abort/durability, ABORTED inspection, acknowledged close and unchanged control are required before completion; completion is not automatic login.

## Private repository and effect gates

`PrivateSessionAccess` supplies an exact lease, origin, access mode, private-store facade and credential transport view. The private facade checks current runtime/lease and independent control before use, detaches mutation lists before suspension, blocks reading or mutating the reserved activation record and rejects unconditional erase. Explicit logout uses the captured retirement workflow. Its read/write result is rejected if the original access becomes stale while storage is suspended.

The transport facade is read-only and incarnation/lease-bound; it never exposes the refresh token. `OFFLINE_PRIVATE` rejects credential access for protected network transport even if an unexpired token happens to remain stored. This mode retains permitted owner data and timer operations, while refusing new background workers. The cooking repository still applies exact pin/recall/generation constraints; offline-private is not broad permission to perform social or purchase commands.

Work admission and callback policies read the independent control record directly and permit only idle/completed state. They recognize ordinary logout, setup discard, distinct `PendingCreate` and composite `PendingSetup`; checking only the ordinary retirement subtype is insufficient. They must not call `LocalRetirementCoordinator.restorationAllowed()` while holding the work-registry mutex: that would invert the coordinator-to-work lock order during logout. Runtime/lease/origin are rechecked after asynchronous policy work. The domain policy must additionally validate the exact current desired ticket and recipe safety state. No native callback may directly display private data or dispatch a remote mutation merely because the OS delivered its UUID.

The retirement coordinator now clears its captured lease and registers its non-cancellable exact process latch before waiting for its own mutex. A suspended capture/restoration read cannot keep outward repository/transport authority alive during a newer logout request. Durable pending still precedes native cleanup. Its first-barrier/power-loss limitation remains unchanged: memory cannot survive process death if every durable barrier write failed.

The Android control-store factory now also requires this opener's successful creation of the previously absent directory before initializing a new idle file. Existing empty, lock-only or lost-all-state directories cannot reset a prior control revision even if all keys are absent. They are repair gates, matching the work-store provenance rule.

## Native evidence and limits

Android integration uses the actual public credential, control, data and work factories, AndroidKeyStore, private SQLite/files, AlarmManager and WorkManager inside `com.feedme.session.test`. The test APK explicitly targets SDK36; running on API35 is not API36-device, physical-device, real app UI or store-build certification. The storage test APK remains target26.

Public factories have fixed Keystore prefixes. Redirecting `noBackupFilesDir` does not isolate those keys. The integration sandbox therefore serializes one fixture, requires every exact fixed namespace to be empty beforehand, creates a new private UUID root, and only cleans the aliases/files it then owns after all store closes and exact native cancellations acknowledge success. Unexpected prior aliases or unacknowledged cleanup preserve the fixture; they are never wiped to make tests green. WorkManager databases and unrelated native requests are not reset or pruned.

Separate instrumentation invocations test explicit WorkManager initialization. Workers use the registry-supplied UUID as their actual WorkRequest ID, with one-day delays to prevent execution; timers are inexact one-day alarms. These fixtures prove exact identity wiring and cancellation, not a complete timer scheduler, foreground worker, reboot receiver or remote domain effect. Production timer receiver mappings must remain stable across upgrades while tickets exist, and worker chains must never cross origins.

Real provider signup/login/reset, account mapping, guest merge, refresh/bootstrap recovery, application UI, user-visible repair, iOS storage/credentials/work, API26-compatible credentials, device lock/restore/power loss, app hard-kill, physical-device and patched SQLite gates remain. No provider credentials, account setup, purchases, store upload or public deployment occurred.

## Verification

Current source-bound verification, exact test totals and the latest receipt are indexed in [Build status — current verification](BUILD_STATUS.md#current-verification). [Session setup publication](SESSION_SETUP_PUBLICATION.md) is DONE bounded; composite tasks 6 and 7 and provider/UI integration remain open. Earlier component counts and hashes below are historical evidence, not current build outputs.

Historical completed snapshot: [planned state recovery](PLANNED_STATE_RECOVERY.md), **DONE for bounded M1.05d.5b.2b.2**, finished **2026-09-13T14:53:52.071Z**. The [historical receipt](verification/planned-state-recovery/verification.json) passes **1,109 Kotlin/server/isolated-PostgreSQL tests, 92 Node checks and 122 native checks**, with zero failures/errors/skips and five clean Android library lint reports. Shared tests total 1,018 (storage 315/session 219); server 46 and PostgreSQL 45 are additional. Native tests comprise 65 storage (57 regular, six separately invoked injected-VFS cases and two separate-process stages) and 57 session. VFS error injection is not an OS errno, physical power-loss or public-factory journal-recovery test. That snapshot preceded runtime data-plan wiring; current progress is linked above, and no whole milestone or release is accepted.

The previous complete [planned private-data activation](PLANNED_STATE_ACTIVATION.md) run finished **2026-09-13T13:35:54.272Z** with **1,069 Kotlin/server/PostgreSQL, 92 Node and 102 native checks**, five clean lint reports, shared 978 (storage 275/session 219), and native 45 storage/57 session (19 credential, ten cancellation, 18 credential-plan and ten integration checks). Those totals and the following composition receipt/counts/hashes remain historical. Confirmed composite startup/abort, integrated interruption acceptance, refresh recovery, unknown-orphan repair and actual UI/provider composition remain open.

The [historical source-bound receipt](verification/session-composition/verification.json), completed **2026-09-13T11:32:50.329Z**, passes **856 Kotlin/server/isolated-PostgreSQL tests, 92 Node checks and 64 Android checks**, with zero failures, errors or skips. Shared Kotlin totals are 765: core 48, contracts 119, transport 72, storage/integration 132, sync 103, kitchen 142 and session 149; server 46 and PostgreSQL 45. All five Android library builds and lint pass with zero lint issues. Existing Kotlin warnings are distinct from clean Android lint.

This adds 43 JVM tests: 22 activation-codec, 20 real encrypted-SQLite runtime integration tests and one pre-mutex retirement regression. The JVM runtime suite uses fake credentials/verifier/native effects and real encrypted data/control/work stores with test JCA vaults. Actual Android credentials/Keystore and OS cancellation are exercised separately by the four integration tests, including one complete runtime path with an explicitly synthetic verifier. No identity provider was contacted.

The API35 arm64 native run includes 29 regular storage tests plus two separate storage-process stages, and 29 credential/cancellation tests plus four separately invoked native integration tests. Controlled close/reopen is not process-hard-kill evidence. The new control-store regression rejects existing empty, lock-only and lost-all-state directories. Delivered-notification cancellation was verified with a temporary grant only to the isolated test package; the runner itself does not change permissions. Both hardlink fixtures were platform-denied, leaving existing-hardlink guard execution unproven.

Source-manifest SHA-256: `e073372a8e9317c84f860f509433dcf115666a73e3f8c152b3d181b3de8c829a` (182 inputs). The immutable attempt `verification/session-composition/attempts/2026-09-13T11-32-03.269Z/` retains 90 evidence files and 13 artifact hashes. Receipt SHA-256: `ba27c2d58ecb4d448a3f03cb2bb37d25ed5b65d662a088db37ae7d5516407402`.

The session test APK is 8,377,719 bytes, SHA-256 `48d3726bc43a13e10ef4a0fa23737a7df27f2a58e7b7f50f91ea4826297616ab`; the storage test APK is 6,257,487 bytes, SHA-256 `26dbe12a6cb762966dd1937520fe670f5000156c4cddc905f6526a1b1f9bff99`. The demo APK is unchanged and was not rebuilt or connected to this runtime. Earlier session-work receipts remain historical and do not cover the new runtime or activation codec.

An independent read-only audit of that snapshot rehashed all 182 then-current source inputs, 13 artifacts and 90 retained evidence files with no mismatch. It matched 856 distinct actual cases from 57 JUnit XML files, all 92 TAP result names and 64 paired native start/success identities to those source declarations, with no failures/skips; 19 native copy pairs and two APK metadata pairs matched. All four commands exited successfully, five retained lint reports have zero issues, and immutable receipt/pointer/last-attempt bytes agreed. Read-only inspection of those APKs confirmed session target36 and storage target26, both minimum26 and compile36; credential support still gates minimum26 at runtime.

After verification, the isolated session test package's original notification denial was restored and confirmed. Owned fixture directories were cleaned by their exact test owners. The headless emulator started for this run exited successfully; none of the 62 retained synthetic PostgreSQL fixture directories has a `postmaster.pid`, and no test-server listener remains on port 8789. No unrelated app data was reset. The delivery-board verifier and its 14 tests pass, retaining 10 milestones, 72 milestone tasks, all 54 features, 162 unchecked feature work packages and 98 screens. No whole feature or milestone was accepted by this continuation.

For the current tree, reproduce with installed JDK17, Android SDK, local PostgreSQL binaries and an owned booted emulator; the original historical composition run used `verify-session-composition.mjs`:

```sh
FEEDME_TEST_DEVICE=emulator-5554 node scripts/verify-startup-recovery-owners.mjs
```

Set `JAVA_HOME`, `ANDROID_HOME` and `FEEDME_POSTGRES_BIN` to installed paths. The runner checks fresh source/artifact/evidence hashes, individual native source-method start/success identities and explicit permission/hardlink branches. A run without notification permission must not be described as demonstrated delivered-notification cancellation. Synthetic verifier tests must not be described as authenticated app sessions or complete feature acceptance.
