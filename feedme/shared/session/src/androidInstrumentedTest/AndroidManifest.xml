<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application>
        <receiver android:name="com.feedme.session.NativeCancellationTestReceiver" android:exported="false" />
        <!-- Deliberately unsafe configuration used only to verify factory rejection. -->
        <receiver
            android:name="com.feedme.session.ExportedNativeCancellationTestReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP" />
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data android:name="androidx.work.WorkManagerInitializer" tools:node="remove" />
        </provider>
    </application>
</manifest>
