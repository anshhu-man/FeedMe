# Interactive progress, basic cookbook and Android timers

14 September 2026 — **VERIFIED as bounded components**, at2026-09-14T05:12:13.803Z. New eighth-batch source changes are not covered by this captured checkpoint.

## Accepted checkpoint

[Receipt](verification/progress-cookbook-timers/verification.json): **2,526 Kotlin/JVM/PostgreSQL tests,192 Node checks,29 Android passed tests and1 separately witnessed interrupted start**. Native coverage is22 timer checks,6 actual progress-host checks and1 fresh-process recovery; the historical354 native suite was not rerun. Nine lint reports contain zero issues. The22 fresh artifacts include eight JVM libraries/eight AARs/four shared test APKs/progress app and test APK; the unchanged historical demo is retained separately.

The source manifest binds542 inputs and784 retained evidence files. Root independently checked2,559 recursive size/hash descriptors across1,370 paths, current/frozen source equality and identical immutable/current/last-attempt receipts. Peer source reviews found no concrete cookbook/timer regression or blocking runner defect. Root visually reviewed all4 fresh1080×1920 PNGs: explicit startup consent, actual request, cooking confirmation and acknowledged cooking header. The controlled process pair preserves the exact plan/session IDs across PID5633→5680 and explicitly resets through the real owner; the interrupted stage is not a passed test or a physical power-loss claim.

- Receipt SHA-256: `1b07f99b42ae4d9d62fea07980451fdb55467ee21c62d0f17e6626aa4645f936`.
- Source SHA-256: `0aa41c28cb20506261fc3006f51c66a5f6cc688c9ebfed4208feacf3cfa581e4`.
- Accepted attempt: `2026-09-14T05-06-58.871Z`; earlier failed attempt preserved.
- The owned headless5556 was stopped after acceptance;301 retained PostgreSQL fixture directories have no root/data `postmaster.pid` files and no test PostgreSQL process remains. User PostgreSQL PID1494 and visible5554/PID60349 were preserved.

This is not full F19/F12, live identity/content/social/provider integration, iOS, background notification, store-ready or publicly-live acceptance. Next implementation: [eighth package](PARALLEL_COOKBOOK_TIMER_MEDIA.md).

The latest user-requested preview run built and linted the separate retained-progress variant successfully, installed it on a newly opened visible emulator5554, and verified the actual meal-request screen. It did not enable the unconnected cookbook/timer/social features. The sixth batch remains a historical source-bound passing checkpoint. New changes below invalidate any claim that its receipt verifies today's changing source tree. The historical demo APK and all historical full-run scripts/receipts stay unchanged.

## Parallel ownership and small tasks

| Lane | Tasks | State / evidence |
| --- | --- | --- |
| Android host | isolated progress variant; positive fresh/existing startup admission; retained session owner; actual request/cooking UI; explicit exit/reset/recovery; installed tests | focused rerun: all6 host checks pass after scroll repair; separate process pair has1 witnessed interruption and1 passed exact recovery/reset |
| Root integration | trusted CONTROL classifier; isolated synthetic identity/catalog; encrypted synthetic-service receipt ledger; canonical paging; integration verification | classifier 6 checks and service 19 checks pass; canonical generation 75 checks pass; source-bound integrated rerun pending |
| Cookbook backend | current principal and new-copy authority; exact immutable snapshots/dedup; default cookbook; six operations; V006; real PostgreSQL tests | server 186 unit and focused 55 PostgreSQL/HTTP checks pass after fixes; no complete F19 claim |
| Android timers | actual wall/elapsed clock; exact same-owner Handler schedule/cancel; foreground delivery gate; actual native runtime tests | focused rerun: all22 native timer checks and25 real-SQLite timer checks pass after bounded read-race repair |

The open user emulator `emulator-5554` now runs `FeedMe · Retained Progress` and must not be used for tests, reinstalled, cleared or stopped. Visible emulator PID60349 uses automatic graphics; software rendering failed to create its window and that earlier process exited. Installed progress APK SHA-256:`6b2842b258bfd038bbd1418942dac08a5e516ece6aabce08f9febe5faa68c9b1`. Root verified `ProgressActivity` is Android's top-resumed activity and visually inspected the initialized meal-request screenshot at `/private/tmp/feedme-emulator-preview.YFUpKC/current.png`. macOS activation was requested but foreground activation was not confirmed. Verification uses the separately owned headless5556. No new provider, spending, permanent app identity, deployment, public push or store submission is authorized by this implementation.

## Explicit contract decisions

- Optional nullable `Collection.nextItemCursor`: absent means legacy complete IDs; null means terminal page; a string requires `getCollection` continuation. `listCollections.nextCursor` still pages collections, not items. Version drift rejects continuation. Bounded pages are not saved-recipe quotas.
- Existing `getCollection` gains canonical Cursor/Limit parameters. Its security aligns with already guest-capable `listCollections` and `getSavedRecipe`: verified account/device or bounded guest identity, always owner-scoped. This fixes guest continuation; it does not enable custom collection writes or F45.
- Save normalizes app-owned acquisition lineage by owner, reviewed recipe version and exact materialized content hash. Scaled variants remain distinct. First public provenance/title is retained; later explicit conflicts are not silent edits. Deletion tombstones one identity, and re-save gets another; old receipts cannot resurrect it.
- `memory.recipe.deleted.v1` contains principalId/savedRecipeId and the committed tombstone aggregate version, never recipe contents. New copies require current positive copy authority; old private copies remain independently owned subject to current owner/recall checks.
- Recalled page contents fail closed until audited versioned lifecycle/redaction is integrated. Make Again true, social-copy grants and custom organization remain unconfigured, not successful no-ops.

## Progress-host truthfulness

Only the separate `progress` variant contains synthetic account/content/service behavior. It uses the actual native encrypted stores and shared controllers; it has no live-auth or Internet-service claim. Synthetic server state/receipts occupy a separate encrypted service record, not a second client command queue. Exact original command keys and outcomes must survive process reopen, or retry remains explicitly unresolved.

Fresh-start eligibility requires positive read-only evidence in this isolated UID (empty/absent native no-backup area and no Keystore aliases), repeated before explicit Start. Native factories retain their own stronger guards. A failed recovery open does not imply freshness. Existing CONTROL inspection is advisory: only the trusted session codec interprets it; the actual runtime rechecks before authentication. Partial/failed owners remain retained, and destructive reset uses real retirement with explicit confirmation.

## Required verification before acceptance

Run focused contract/classifier/service tests, then real host and timer instrumentation on a separate device, plus backend unit/real-PostgreSQL tests. Bind a new source manifest and actual artifacts/results; retain failed attempts. Inspect launcher/UID/permissions and screenshots. No complete app, 54-feature, iOS, background alarm, provider, live social or release-readiness claim follows from this package alone.

## Focused evidence and fixes so far

Logs are retained under [focused verification](verification/progress-cookbook-timers/focused/). These are changing-source diagnostics, not a single accepted source-frozen receipt.

- `11-progress-build.log`: progress app/test APK build and 28 JVM checks pass. Packaged identity is `com.feedme.development.progress`; no Internet, network-state, wake-lock, boot or foreground-service permissions; WorkManager auto-initialization is absent. The historical demo SHA remains unchanged.
- `12-progress-native.log`: actual launcher consent/recreation reaches an acknowledged exact cooking session, but its visible-heading assertion fails because the prepared-recipe scroll offset survives navigation. Root keyed the shared screen's scroll state to screen/session; the assertion remains strict for the rerun. Close/reopen, missing-service reset, exact pending-setup abort and cancellation/close-retention checks passed. Three actual screenshots were retained and visually inspected; the missing cooking screenshot is not counted.
- `13-cookbook-fixed.log`: 186 server unit and 55 focused real-PostgreSQL/HTTP checks pass. Fixed actual Ktor route/query parameter merging and refused nondefault collection writes/replays. Fixture scaling now explicitly declares reviewed scaling bounds; CIO framing checks accept the actual HTTP/1.0 or HTTP/1.1 400 response while retaining zero-effect assertions. V001–V005 match the historical receipt byte-for-byte; V006 is append-only.
- `09-timer-runtime-diagnostic.log`: exact pause cancellation leaves its sibling ARMED/DUE/acknowledged but its callback becomes BLOCKED. The repair retries only advancing revision mismatches from successful read observations, at most three times, with all current gates rechecked. No failed read, work commit, unknown result or invoked effect is retried. This still needs native regression evidence.

The new checkpoint runner will distinguish 29 normal native successes from one externally witnessed process interruption if all proposed tests pass. It will not claim that the historical 354-test Android suite was rerun. Controlled process stop/reopen does not establish physical power-loss or unknown-COMMIT behavior.

### Subsequent focused evidence and first full attempt

- `15-native-fixes-build.log`, `16-timer-fixed-native.log`, `17-node-checks.log`, `18-progress-fixed-native.log`, and the19/20 process-stage logs retain subsequent focused results. The actual native cooking header is visible after navigation; the repaired timer callback preserves its unchanged sibling. Node regression total is192.
- Full attempt `2026-09-14T04-47-25.968Z` ran all2,526 JVM/PostgreSQL checks successfully but failed progress lint because the manifest's Startup provider was only a runtime dependency. It did not reach the native-install phase. This failed attempt remains immutable and is not acceptance.
- The narrow fix declares `androidx.startup:startup-runtime:1.2.0` explicitly for progress, matching the already-selected runtime version; it does not suppress lint, upgrade the version or remove other initializers. The latest preview command `:apps:android:assembleProgress :apps:android:lintProgress` passed with `No issues found`.
- A fresh combined source-frozen run is required; no full seventh acceptance is inferred from these separately timed focused results.
