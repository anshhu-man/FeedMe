package com.feedme.development.progress

/** One Activity attachment; construction never creates storage, work or delivery authority. */
internal class ProgressForegroundHost internal constructor() {
    override fun toString() = "ProgressForegroundHost(<opaque>)"
}
