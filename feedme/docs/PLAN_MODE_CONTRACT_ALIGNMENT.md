# Unresolved meal plans — draft contract alignment

14 September 2026. This is a local draft-contract correction, not a deployed API migration or feature completion.

An automatic request can legitimately produce `needsConfirmation` or `noMatch` without selecting `cook`, `assemble` or `improve`. Requiring a concrete mode in those responses forced the canonical adapter to return a conflict instead of the documented domain state. The Plan schema now makes `mode` conditional: required for `ready`/`recalled`, optional for the two unresolved statuses. Present values retain the original three-value enum; explicit null, `auto` and unknown values remain invalid. Absence never authorizes cooking. No operation, feature or V1 scope was added or removed.

The schema uses OpenAPI 3.1 JSON Schema `allOf`/`if`/`then`, supported by both existing validators. `PlanWire.mode` preserves presence as `WireField<String>`. The deterministic planning adapter omits an unresolved value and still rejects a READY decision without its mode and recipe. Shared/server regression matrices check both validators, exact wire retention, valid concrete values and invalid absent/null/auto cases.

Canonical source SHA-256 changed intentionally from `f0a5f20c4a84822813137c3e6b1ef31ec22afd1d7ee384f7f29b5df09e10aba0` to `73e23af85fd128acdb8e1c630d7ac9de587233c3d6aa233cad3c18394bc1afbf`. Current generation/server/test pins were reviewed and refreshed. Historical verification receipts and the historical generator-spike experiment remain pinned to their original source; they are not evidence for the new contract.

F02 prose now states unresolved-mode behavior. F09 prose was aligned with its existing canonical operation: `nextPlan` uses `Idempotency-Key`, `X-Device-Session` and `AdaptRequest` constraints/cursor/exclusions; it does not use `If-Match`. The immutable parent and owner/request-bound traversal provide the existing lineage semantics. This correction does not introduce a second endpoint convention.

Focused verification passed75 generator checks; shared core54/contracts123/planning40; and the then-current server71/PostgreSQL99 tests. The final [second-batch source-bound run](PARALLEL_MEAL_STARTUP.md) passed at2026-09-13T22:19:20.584Z, including the final server71/PostgreSQL101 and all updated shared tests. The server's full schema differential corpus now has8,421 systematic mutations: the two unconditional-required checks for Plan.mode (Plan and its PlanPage item) become conditional-branch cases. Explicit matrices cover all four statuses, each concrete mode, omission, null, auto and wrong types. The first focused runs exposed an obsolete expected-conflict assertion and the old8,423 count; their intended semantics were reviewed and updated, not hidden or skipped.

The regenerated local blueprint passed22 browser scenarios, rendered98 screens and exercised900 action handlers with no runtime errors or attempted HTTP requests at2026-09-13T21:47:52.699Z. Structural validation passed137 checks, and the final rebuild embeds that receipt. This is synthetic HTML navigation evidence, not native feature/backend acceptance. A sandbox-denied browser launch was retried using an isolated permitted headless launch; no existing user browser session was used.
