# Cookbook client and photo processor — ninth package

14 September 2026 — **VERIFIED AS BOUNDED COMPONENTS**, not complete F19, deployed media processing or a release. The third source-frozen attempt passes at2026-09-14T09:00:46.781Z:2,855 Kotlin/JVM/PostgreSQL tests,202 Node checks,47 native passes and two separately witnessed interrupted starts. All nine lint reports are clean. [Accepted receipt](verification/cookbook-processor/verification.json). The [eighth checkpoint](PARALLEL_COOKBOOK_TIMER_MEDIA.md) and all failed ninth attempts remain unchanged historical evidence.

## Owned small tasks

| Owner | Exclusive implementation | Concrete outcome |
| --- | --- | --- |
| McClintock | Shared mealflow cookbook controller/records/public composition, app UI and native tests | Exact selected Plan Save, original-receipt recovery, remote paging/local search, saved detail and confirmed delete on the actual retained kitchen |
| Averroes | Shared kitchen saved-recipe preparations/paging/cache/tombstone primitives and tests | Typed bounded receipt mutations, capacity checks, nonresurrection fences and exact acknowledgement support; agreed seam with McClintock |
| Nash | Server processing models/ports/store/coordinator, V008 and narrow delete integration/tests | Durable event ingestion and leases, exact object write/cleanup intents and gated READY/REJECTED with real PostgreSQL tests |
| Lead | Initial server codec/child-process implementation; synthetic progress cookbook service, integration, evidence and tracker updates | Real bounded JPEG/PNG transformation and separately labelled preview Save/read/page/delete; centralized tests |

The second full attempt failed on an incorrect decorative-node test target; focused15 proved the corrected exact action check and the third full attempt passes its complete frozen scope. Root and independent audits agree on source identities and retained hashes. Root has released editing for the [tenth dispatch](PARALLEL_RECALLED_COPY_DRAFTS.md); this immutable ninth baseline does not cover those new edits. All Gradle, database and device runs are centralized. User emulator5554 remains the installed seventh preview and is not a test target. No agents are claimed beyond the four actual workspace workers.

After their initial implementations, Averroes owns codec review/regressions and independent preview-service review; Nash independently reviews cookbook command/finalization behavior. Source freezes are per build dependency set. The previous user turn made progress by confirming the running native preview, capturing its actual screen and successfully foregrounding its existing macOS emulator window; no app data or APK was replaced.

## Implementation requirements

Follow [cookbook integration](COOKBOOK_CLIENT_INTEGRATION_PLAN.md) and [media processing](MEDIA_PROCESSOR_EXECUTION_PLAN.md). SavedRecipe is never a Plan. Save requires the exact acknowledged selection and remains distinct from Make Again, taste feedback, paid organization and social publication. No implicit dispatch on render, restoration or Back. Preserve queue/domain acknowledgement, recalls, owner/lease/generation, original command and per-ID delete fences.

The processor accepts only exact committed photo lineage. Decode success is neither malware clearance nor moderation approval. Mandatory configured object/safety/worker-authority ports have no accepting production defaults. First codec configuration admits JPEG/PNG, emits explicitly configured PNG derivatives, and does not advertise HEIC, avatar or clip support. Real child timeout/heap/concurrency limits are local evidence; deployment OS/network isolation remains a separate gate. Main remains health-only without configured product services.

V001–V007 and historical verification scripts/receipts stay unchanged. An additive V008 and a reviewed same-transaction delete hook must not invalidate V007's immutable delete/completion receipt semantics. No broad object cleanup or database lock held over external I/O.

## Accepted scope and remaining release gates

The accepted checkpoint includes fresh Kotlin/common/server and real PostgreSQL tests, actual codec bytes/metadata/orientation/process-failure tests, configured coordinator integration, real retained Android cookbook UI/recreation/recovery, source/artifact freeze, native manifests and screenshots. Object/safety/identity/catalog/service fixtures remain explicitly synthetic; there is no deployed processor or accepting moderation default. Full background timer, recalled-copy removal, saved-source planning/Make Again, signed content-manifest, live providers, iOS/physical-device and release gates remain open. V1 remains44 included features and10 deferred features; the full54-feature blueprint is intact. Focused failures stay retained and are not rewritten as passes.

Existing provider/release questions in [USER_ACTIONS.md](USER_ACTIONS.md) remain unanswered; the new U13 diagnostic-data retention choice is tracked separately. No new provider, budget, identity, public push, deployment or submission is authorized. The existing needs-you follow-up is retained without duplicate alarms.

## Focused evidence — not ninth acceptance

- `verification/cookbook-processor/focused/01-codec-tests.log`: BUILD SUCCESS;18 exact codec methods pass, including a real JVM worker, forced timeout and cancellation. Original XML retained in `01-codec-results/`. The first production media store/coordinator sources also compile; no database/worker acceptance follows.
- Root review found lease checks preceding potentially slow authority callbacks in the new backend. The owner added fresh original-token/expiry checks after the last callback/lock before renewal, intent creation/write admission and other stage returns. These changes still need the planned real-database short-lease regression; a source fix is not proof of race coverage.
- `02-kitchen-codec-tests.log`: BUILD SUCCESS; all234 kitchen methods in12 XML classes (201 existing +33 new saved-recipe cases), and27 codec methods pass. All counts have zero failures/errors/skips. Added codec cases exercise actual structured GPS/serial/thumbnail metadata, Adam7, grayscale/opaque palette transparency, truncated JPEG entropy, blocked large-input pipes and malformed worker envelopes. [Codec implementation/limits](PHOTO_CODEC_IMPLEMENTATION.md).
- Independent review identified cross-boundary concurrency risk in the new process-local negative-evidence registries. Repository and mealflow owners are replacing global mutable-list membership with immutable-list atomic CAS and adding actual concurrent-boundary tests. Consequently the evolving source is not frozen at the second focused run.
- `03-cookbook-controller-build.log`: initial main controller compiles for JVM and Android;195 existing mealflow tests pass. The new kitchen JVM class fails JUnit initialization because two expression-body methods return a non-Unit result. This is retained in `03-failure/`, not counted as a pass. Explicit `runBlocking<Unit>` fixes both methods.
- `04-kitchen-server-postgres.log`: all236 kitchen tests across13 XML classes pass, including35 added saved-recipe/concurrency tests. Original XML retained in `04-kitchen-results/`. Server unit test compilation fails on one sealed-type `assertNotEquals` inference; the assertion is corrected with the explicit common type. Integration test sources compile, but no PG test executes in this failed run.
- Independent codec review finds that JDK process destruction itself may block on pipe cleanup and PNG tRNS unused high bits must be masked. Termination/wait/cleanup now live entirely in a retained reaper with bounded caller wait; three real grayscale/RGB/low-depth pixel regressions and a strengthened blocking-destroy lifecycle double bring codec methods to31. Those changing cases still require the next run.
- `05-cookbook-app-progress.log`: app mains compile for JVM/Android and all109 app presentation tests pass, including8 new cookbook tests. New progress instrumentation sources compile and an Android test APK assembles, but no device test runs. Mealflow test compilation fails on an omitted explicit null response body; fixed. Progress service runs43 tests with9 failures: a synthetic license label incorrectly used as a canonical enum is rejected by the actual response binder. The preview now uses canonical `privateCopyOnly`, still explicitly synthetic, and must be retested. Original app/progress XML retained in `05-results/`. An earlier handoff arithmetic total of129 was corrected against the eight XML headers;109 is the actual count.
- Root adds a scoped synthetic cookbook ledger beside existing preview state: exact materialized own-Plan saves, first-copy dedup, confirmed-version deletion and lineage-fenced replay; bounded exact-filter/snapshot paging; additive legacy interpretation with no writes on open/read. Eleven new service tests cover these paths. This is not a production copy-rights or signed-cursor provider. Backend source now also sanitizes cleanup-provider exceptions and adds one grouped real-PG regression (31 processing/392 total expected); counts are expectations until executed.
- `06-server-postgres.log`: all392 real-PG tests across18 classes pass, including31 new processing cases; the latter run real PostgreSQL and include actual JPEG/PNG child decoding with explicitly synthetic object/safety adapters. Server unit tests run250 across25 classes with1 failure: actual depth1 PNG transparency remains opaque. Review traces this to JDK17 expanding packed gray samples before comparing unscaled tRNS; the normalization fix and all-sample low-depth regression are in progress. Original XML is retained in `06-results/`; this failed combined run is not accepted as ninth completion.
- Independent preview-service review finds no concrete blocker in the exact-copy/replay/page/legacy implementation. Independent controller review does find uncached lifecycle refresh/version handling and the last cancellable caller-return finalization gap; the UI owner is fixing both with regressions before the next client run. The prior sibling-command test is being upgraded from a cooking proposal to an actual queued command. The recalled-copy deletion target remains a precise follow-on documented in the integration plan, without weakening redaction.
- `07-server-codec.log`: BUILD SUCCESS; all250 server unit methods across25 XML classes pass, with original XML in `07-results/`. All31 codec methods pass, including every22 possible gray sample across1/2/4 bits in both derivatives. The low-depth normalization now both masks and expands the sanitized comparison sample to match JDK17, without changing source/compressed pixels. This is fresh focused unit evidence; the PG run06 used the preceding codec revision and complete source-frozen acceptance still remains.
- Two more preview-service tests are added for response-byte-aware page continuation and retained identity-capacity denial with exact failure replay (13 added/45 total progress methods expected). A separate actual native saved-copy process-restart pair is being added; neither new test set has yet passed a run.
- `08-cookbook-client-progress.log`: all45 preview-service methods and109 app presentation methods pass; the current development APK assembles but is not installed. All231 mealflow methods run, with15 failures in the new32-method cookbook class;195 existing +4 new readiness +17 cookbook methods pass. Original XML retained in `08-results/`. Root identifies the incorrect `feedme.command.meta` archive namespace—the actual queue uses `feedme.command.metadata`—and the owner is fixing that real integration defect plus exact failing fixtures. The batch remains unaccepted until its real controller/native paths pass.
- `09-cookbook-client-native-compile.log`: queue archive capture now matches the actual metadata namespace; all but1 of231 mealflow methods pass, while45 progress and109 app methods pass again. Both current progress APK and Android test APK assemble, including the separate cookbook process pair. The remaining test incorrectly expects an independent Save lane to block behind cooking: actual queue policy is FIFO **per family**, not global. It is being corrected to prove the intended invariant—exact prior cooking command remains unchanged and undispatched—while a separately authorized Save can finish. No queue policy is weakened. Original XML retained in `09-results/`; still no native execution or ninth acceptance.
- [10-cookbook-client.log](verification/cookbook-processor/focused/10-cookbook-client.log): BUILD SUCCESS in31s; all231 mealflow methods freshly pass after correcting that cross-lane test. It now proves that a separately authorized Save can finish while the exact queued cooking command remains unchanged and undispatched. The production archive namespace defect was fixed before run09; run10 verifies the final controller/test snapshot without changing per-family queue policy. App109 and progress45 tasks reuse cached results from09, so they are not fresh executions in10.
- [11-node-parsers.log](verification/cookbook-processor/focused/11-node-parsers.log): all202 Node checks pass, including10 new ninth-runner parser regressions. Root and Averroes independently reviewed the frozen `verify-cookbook-processor.mjs` runner without finding a concrete blocker. Its planned47 native passes and two separately witnessed interruptions are expected scope, not executed results.

## First full attempt — failed, partial evidence retained

The [08:01:34.889Z attempt](verification/cookbook-processor/attempts/2026-09-14T08-01-34.889Z/report.json) finished at2026-09-14T08:08:34.280Z with `passed=false`. Fresh Gradle completed in5m25s with634 executed tasks, including all45 explicitly required tasks. Independent audit matches all2,855 exact JVM/PostgreSQL identities across149 XML classes:2,168 shared +45 progress +250 server +392 PG, with zero failures/errors/skips. All202 exact Node identities pass;22 fresh artifacts, six APK metadata records and nine zero-issue lint reports match their retained copies. The release-scope report matches its current inputs and remains a structural44-included/10-deferred check, not feature acceptance.

The native progress-host invocation then fails `bActualLauncherRequiresConsentAndRetainsCookingAcrossActivityRecreation` at `AndroidProgressHostTest.kt:117`: **Missing explicit progress action: Review prepared start**. Its [transcript](verification/cookbook-processor/attempts/2026-09-14T08-01-34.889Z/progress-host-tests.log) reports six tests with one failure. The runner correctly rejects this despite adb exit0 and instrumentation terminal-1. That checkpoint did not establish a fixture-timing explanation or production fix; subsequent focused diagnostics are recorded below. No native cookbook or complete ninth acceptance follows from the earlier successful stages.

The failed checkpoint binds603 frozen inputs, source SHA-256 `29fa1d3534c16f89e5868e078b3b1653788d8ce078c21098c9fa54da4cdc8a7c`. At audit time, all frozen/current source copies and2,803 recursive hash descriptors across1,497 paths match;850 evidence entries are retained. Failed receipt SHA-256:`734af6bc245d96629d4990fb028402776bfbf67aebbafb82f5aed41a7c2d696f`. Immutable report and last-attempt agree; no ninth accepted `verification.json` exists. This is a fresh partial checkpoint, not ninth acceptance or coverage of later repairs. Earlier failed logs remain retained; no old runner or receipt was edited. The installed seventh user preview and all production/provider/release gates remain unchanged.

## Focused12/13 — separate incomplete native suites

[12-native-scroll-build.log](verification/cookbook-processor/focused/12-native-scroll-build.log) reports BUILD SUCCESS in1s:16 tasks executed and233 up-to-date, not a fresh complete rebuild. The original APKs used by focused13 are retained unchanged under `focused/13-native/artifacts/`: main APK SHA-256 `b14f246d50a42618783e6014e68efd5196bf8c7635d83bbc38447d7c5bc16912`; test APK SHA-256 `cb20dea82bd4876bfd9aedf3e7fb953f75c56c4577ae528a6aafc2c53e12c665`.

- [Progress host](verification/cookbook-processor/focused/13-native/progress-host.log): six methods run, five pass and b fails at line128 on the new immediate-absence assertion after a forced scroll. Stronger controller/navigation invariants pass before that assertion. Retained accessibility text and the [cooking failure PNG](verification/cookbook-processor/focused/13-native/diagnostics/progress-host-ui-diagnostics/cooking-consent-failure.png) show the correct phase and actual bottom of the page. A condition-based await has been added but is not yet tested; the suite remains failed.
- [Timer host](verification/cookbook-processor/focused/13-native/timer-host.log): four methods run, three pass and d fails because the observed state is `CHECKING`, not expected `DELIVERED`. Averroes's coherent-observation fix is source-ready but not yet compiled or verified. Diagnostics remain retained; no passing whole timer suite or background-delivery guarantee follows.
- [Cookbook host](verification/cookbook-processor/focused/13-native/cookbook-host.log): four methods run, three pass and d fails its disabled-action assertion at line182. McClintock is diagnosing the semantics before a fix is accepted. Root viewed all four retained cookbook captures—saved copy, page, restored copy and delete consent—under `focused/13-native/captures/progress-cookbook-ui-evidence/`. Those captures and passing methods are focused evidence, not acceptance of the failed suite.

All13 logs, source manifest, diagnostics, captures and original APKs remain retained. Its separate `focused/13-native/report.json` records14 methods:11 pass and three fail, not current acceptance or a combined passing suite. No process-restart test ran in13. The owned13 emulator5556 stopped with exit0, followed by a fresh empty owned5556 for the next full run. The user's5554 and its seventh accepted APK remain unchanged. The following second attempt records the later helper fixes; it does not rewrite13 evidence.

## Second full attempt — failed cookbook test, not ninth acceptance

The [08:23:48.057Z attempt](verification/cookbook-processor/attempts/2026-09-14T08-23-48.057Z/report.json) finished at2026-09-14T08:34:29.951Z with `passed=false`. Gradle freshly executes634 tasks in5m7s, including all45 required tasks. Independent audit matches2,855 exact JVM/PG methods across149 XML classes (2,168 shared +45 progress +250 server +392 PG),202 exact Node identities across12 files,22 fresh artifacts, six APK metadata records and nine clean lint reports. All test failures/errors/skips are zero in those JVM/Node stages. Structural release scope remains44 included/10 deferred, with current input hashes matched.

Four complete native invocations pass41 exact source-bound methods: six timer-only cancellation,25 foreground timers, six progress host and four timer host. Root viewed all eight retained progress/timer PNGs. These are successful stages within a failed full run, not ninth acceptance. Cookbook a/b/c pass, but d fails its semantics await at line183; none of that failed invocation is added to the41-stage total. Its [transcript](verification/cookbook-processor/attempts/2026-09-14T08-23-48.057Z/progress-cookbook-host-tests.log) reports four tests/one failure. Actual accessibility evidence shows the real Save action disabled while an enabled decorative node labelled Button is selected by the test. This identifies the incorrect test target; it does not establish an enabled production Save action or accept the forthcoming correction. No process-interruption/recovery stages execute. Root's four cookbook PNG reviews remain the separate focused13 captures, not new second-attempt cookbook captures.

The checkpoint freezes603 inputs with source SHA-256 `3f33005e088ef67144f17035ac328c02abe89ba4e16a0ccd319881dce625aa7b`. Only the three native host-test source files differ from the first frozen attempt; production source is unchanged between those snapshots. Failed receipt SHA-256:`8e10de8eac1e9d0e37022619841e8c4e42298a6de5aa40e7b91ca6079c07c9b3`. Audit reconciles2,843 recursive descriptor references across1,517 paths and870 evidence entries. Retained/live XML and artifacts match; immutable report equals last-attempt and no accepted ninth `verification.json` exists. The runner records source unchanged through completion. At the later independent audit, the current cookbook-host test had already changed after freeze release: its exact retained source copy, not the edited working file, is the authority for this result. Subsequent source/copy fixes require another complete run. Installed seventh preview, provider gates and release status remain unchanged.

## Focused14/15 and third-run start — retained history

[14-cookbook-action-build.log](verification/cookbook-processor/focused/14-cookbook-action-build.log) reports BUILD SUCCESS in6s:28 tasks executed and398 up-to-date. This incremental build is not a fresh full test run. Root also corrects one START-screen sentence to acknowledge basic cookbook Save, browsing and confirmed removal through the explicitly synthetic service; Make Again and sharing remain disconnected. This copy change does not alter the user's installed seventh APK or imply production identity/content.

[Focused15](verification/cookbook-processor/focused/15-cookbook-action/report.json) passes exactly `AndroidProgressCookbookHostTest#dUnsavedFormBlocksActualSaveAndBackNeverCreatesAnotherCopy` in16.461s, verified at2026-09-14T08:49:09.144Z. Its603-input source snapshot is unchanged during the run; original log and main/test APKs are retained. The corrected test selects the actual disabled Save action rather than the enabled decorative node. This is one focused regression, not the full four-method cookbook suite, process-restart evidence or ninth acceptance.

The previous owned5556 process exited0 after evidence retention; a fresh owned5556 then started the third full attempt at `verification/cookbook-processor/attempts/2026-09-14T08-50-11.189Z`. Its result remained pending until the actual terminal receipt below. Earlier failed full/focused evidence stays unchanged; user5554 was not touched.

## Third full attempt — accepted bounded checkpoint

The [immutable third receipt](verification/cookbook-processor/attempts/2026-09-14T08-50-11.189Z/report.json) passes at2026-09-14T09:00:46.781Z. All634 Gradle tasks execute in5m9s, including45 required tasks. Independent audit matches2,855 unique source-declared methods across149 fresh XML classes:2,168 shared,45 progress,250 server and392 real-PG. All202 Node identities across12 files pass. Twenty-two fresh artifacts, six APK metadata records and nine zero-issue lint reports match current and retained bytes.

Seven complete native invocations pass47 exact methods: six timer-only cancellation,25 foreground timers, six progress host, four timer host, four cookbook host and two fresh-process recoveries. Two stage-A methods are witnessed interrupted starts, never passing tests; stage B separately restores exact timer or saved-copy lineage and explicitly resets/closes its owner. Independent parsing matches canonical Long markers, current UID/live PID before the owned stop, PID disappearance, fresh-process identity and unchanged original-marker repulls. Root viewed all12 source-bound PNGs from this accepted attempt. These prove real retained native storage/controller/UI with synthetic service/identity, not production login, copy rights, interrupted transactions, physical power loss or background notification delivery. The historical354-test native/VFS matrix was not rerun.

Source SHA-256:`6173cde4f7baaf2dcae6af2067c93fecebca719880851ba38c2ed2197ea793a7` over603 inputs; receipt SHA-256:`840990ff363394aa3ae152616ba92e84659f9ab293452ee4c31e2c996df504ab`. Initial/final manifests and every current/frozen source match. Root and independent audits reconcile2,909 recursive hash descriptors across1,552 paths and905 retained evidence entries. Immutable/current/last-attempt receipt bytes agree. Historical runners, demo APK and earlier failed attempts are preserved. Root stopped only the owned test5556 process, exit0; its read-only process check found no instrumentation, test PostgreSQL or verifier processes, leaving the user's existing PostgreSQL and visible5554 untouched.

## Next bounded package and operational retention

The actual [tenth dispatch](PARALLEL_RECALLED_COPY_DRAFTS.md) combines [recalled-copy removal](RECALLED_COPY_REMOVAL_PLAN.md) with canonical PostDraft persistence: Averroes owns authentic local targets/redacted evidence/exact204 application; McClintock owns original intent, consent, UI and native tests; root owns retained synthetic recall, configured HTTP regression and integration; Nash reviews shared boundaries and owns the PostDraft authority/schema proposal followed by bounded server persistence. None of that new work is accepted by the ninth receipt. The cloud-only/stale-version removal case still requires an approved content-free canonical contract. No invented endpoint, unredacted recipe, re-based uncertain command or whole-F19 completion follows. [Team split](AI_TEAM_PLAN.md). All44 V1 features remain in scope.

A read-only08:59:21.946Z inventory found407 exact nonsymlink `/private/tmp/feedme-postgres-test.<random>` directories, all current-UID-owned0700 and matching the disposable PostgreSQL15 configuration/own socket markers. They occupy106.565GiB (114,423,013,376 allocated bytes) across3,080,510 files; none had `postmaster.pid` or `initdb.pw`, and no credential contents were read. Free space was99.35GiB. This motivates a separate explicit retention/cleanup task before repeated full runs, not permission to delete anything. [U13](USER_ACTIONS.md) asks whether to archive locally/remove uncompressed copies or keep them and awaits the user's response. Any future cleanup must revalidate exact ownership/configuration and stopped processes, preserve selected failed/recent diagnostics, and follow an approved retention policy. No directories were deleted, moved or archived by this audit.
